<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reservation</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel='stylesheet' type='text/css' media='screen' href='main.css'>
</head>
<body>
    <div class="center">
        <form id="movieForm" method="post">
            <div class="form-group">
                <label for="nameInput">Name</label>
                <input type="text" class="form-control" id="nameInput" name="name" value="<%= data.name%>" placeholder="Name: ">
              </div>
              <%if(errors.name) {%>
                <div class="alert alert-danger " role="alert">
                    <%= errors.name_text%>
                  </div>
            <%}%>
            <div class="form-group">
              <label for="emailInput">Email address</label>
              <input type="text" class="form-control" id="emailInput" name="email" value="<%= data.email%>" aria-describedby="emailHelp" placeholder="Email:">
              <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
            </div>
            <%if(errors.email) {%>
                <div class="alert alert-danger " role="alert">
                    <%= errors.email_text%>
                  </div>
            <%}%>
            <div class="form-group">
                <label for="dateInput">Date</label>
                <input type="date" class="form-control" id="dateInput" name="date" value="<%= data.date%>" placeholder="Date:">
            </div>
            <%if(errors.date) {%>
                <div class="alert alert-danger " role="alert">
                    <%= errors.date_text%>
                  </div>
            <%}%>
            <div class="form-group">
                <label for="seatInput">Seats</label>
                <input type="number" class="form-control" id="seatInput" name="seats" value="<%= data.seats%>" placeholder="Seats:">
            </div>
            <%if(errors.seats) {%>
                <div class="alert alert-danger " role="alert">
                    <%= errors.seats_text%>
                  </div>
            <%}%>
            
            
            <button type="submit" class="btn btn-primary">Submit</button>
          </form>
    </div>
</body>
<script> export var name_alert = document.getElementById('name_alert')</script>
</html>